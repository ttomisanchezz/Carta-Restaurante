# dependencias
/node_modules
/.pnp
.pnp.*

# build de Next
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# tests
/coverage
/playwright-report
/test-results
/blob-report
/.playwright

# supabase local
/supabase/.temp
/supabase/.branches

# entorno
# .env* atrapa todo lo que tenga valores reales...
.env*
# ...y estas dos lineas lo devuelven: son los unicos archivos de entorno que SI se commitean.
# La negacion va DESPUES del patron que anula, si no, no tiene efecto.
!.env.example
!.env.production.example

# marcador del copiado de workspace/ (ver §10 Bootstrap): es estado local, no historia
.workspace-applied

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel

# NOTA: blueprints/ NO se ignora. El bundle de diseno se commitea con el proyecto.
# Las herramientas que recorren el arbol (biome, tsc, vitest, playwright) lo excluyen
# cada una en su propia config; ver blueprint.md §19.6.
